import React from "react";
import classes from './Auth.module.css';

const Auth = (props) => {
    return (
            <div className={classes.Auth}>
                <h1>Autorisation</h1>
            </div>
    )
}
export default Auth;